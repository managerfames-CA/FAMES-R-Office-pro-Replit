{
  "name": "bybit-insw-bot-workspace",
  "version": "1.2.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build:ui": "pnpm --filter @workspace/trading-terminal build",
    "typecheck": "pnpm --filter @workspace/trading-terminal typecheck",
    "build": "pnpm run typecheck && pnpm run build:ui",
    "test:backend": "PYTHONPATH=backend pytest -q backend/tests"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "packageManager": "pnpm@10.26.1"
}
