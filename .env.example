# Non-secret runtime settings used by this release.
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# No DATABASE_URL, SESSION_SECRET, API key, or admin credential is consumed
# because the approved source-of-truth application has no server database or login backend.
