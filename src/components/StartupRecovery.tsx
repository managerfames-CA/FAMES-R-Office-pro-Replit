export function StartupRecovery({ message, onExport, onReset }: { message: string; onExport?: () => void; onReset?: () => void }) {
  return <main className="recovery-screen"><div className="panel narrow"><h1>Unable to start</h1><p>The app stopped safely before loading operational data. Existing module data has not been changed.</p><div className="alert error" data-testid="startup-error">{message}</div><div className="form-actions">{onExport&&<button className="button secondary" onClick={onExport}>Export Raw Recovery Snapshot</button>}{onReset&&<button className="button danger" onClick={onReset}>Reset Startup Configuration</button>}<button className="button primary" onClick={() => window.location.reload()}>Reload Application</button></div><p className="muted">Reset Startup Configuration affects settings and application metadata only. A local recovery snapshot is created first.</p></div></main>;
}
